# C0 — Export truth table (WorldProject → export artifacts)

Generated by `packages/export-ai-rpg/src/__tests__/c0-export-table.test.ts`. Do not hand-edit.

Schema version 4.5.0. Tally: {"no-channel":194,"carried-lossless":168,"carried-approximated":14,"carried-garbled":1}.

| Field | Class | Channel | Image | Note |
|---|---|---|---|---|
| `ambientLayers[].color` | no-channel | none | — | Ambient layers (fog / rain / dust) have no pack field. |
| `ambientLayers[].id` | no-channel | none | — | Ambient layers (fog / rain / dust) have no pack field. |
| `ambientLayers[].intensity` | no-channel | none | — | Ambient layers (fog / rain / dust) have no pack field. |
| `ambientLayers[].name` | no-channel | none | — | Ambient layers (fog / rain / dust) have no pack field. |
| `ambientLayers[].type` | no-channel | none | — | Ambient layers (fog / rain / dust) have no pack field. |
| `ambientLayers[].zoneIds[]` | no-channel | none | — | Ambient layers (fog / rain / dust) have no pack field. |
| `assetPacks[].author` | carried-lossless | assetPacks | `assetPacks[].author` |  |
| `assetPacks[].compatibility.engineVersion` | carried-lossless | assetPacks | `assetPacks[].compatibility.engineVersion` | The AUTHOR-declared engine version, distinct from the hard-coded `2.0.0` the exporter writes into the manifest. |
| `assetPacks[].compatibility.minSchemaVersion` | carried-lossless | assetPacks | `assetPacks[].compatibility.minSchemaVersion` |  |
| `assetPacks[].description` | carried-lossless | assetPacks | `assetPacks[].description` |  |
| `assetPacks[].id` | carried-lossless | assetPacks | `assetPacks[].id` | Same pass-through channel as assets. |
| `assetPacks[].label` | carried-lossless | assetPacks | `assetPacks[].label` |  |
| `assetPacks[].license` | carried-lossless | assetPacks | `assetPacks[].license` |  |
| `assetPacks[].source` | carried-lossless | assetPacks | `assetPacks[].source` |  |
| `assetPacks[].tags[]` | carried-lossless | assetPacks | `assetPacks[].tags[]` |  |
| `assetPacks[].theme` | carried-lossless | assetPacks | `assetPacks[].theme` |  |
| `assetPacks[].version` | carried-lossless | assetPacks | `assetPacks[].version` |  |
| `assets[].id` | carried-lossless | assets | `assets[].id` | The whole `assets` array is passed through untouched into ExportResult.assets — a World Forge re-import channel with no engine reader. |
| `assets[].kind` | carried-lossless | assets | `assets[].kind` |  |
| `assets[].label` | carried-lossless | assets | `assets[].label` |  |
| `assets[].packId` | carried-lossless | assets | `assets[].packId` |  |
| `assets[].path` | carried-lossless | assets | `assets[].path` |  |
| `assets[].provenance.author` | carried-lossless | assets | `assets[].provenance.author` |  |
| `assets[].provenance.createdAt` | carried-lossless | assets | `assets[].provenance.createdAt` |  |
| `assets[].provenance.license` | carried-lossless | assets | `assets[].provenance.license` |  |
| `assets[].provenance.source` | carried-lossless | assets | `assets[].provenance.source` |  |
| `assets[].tags[]` | carried-lossless | assets | `assets[].tags[]` |  |
| `assets[].version` | carried-lossless | assets | `assets[].version` |  |
| `author` | no-channel | none | — | Project author is not exported. Scoped: asset-level `provenance.author` is a different field that happens to hold the same value here — unscoped, that collision reads as carriage. |
| `buildCatalog.archetypes[].description` | carried-lossless | contentPack | `buildCatalog.archetypes[].description` |  |
| `buildCatalog.archetypes[].grantedVerbs[]` | carried-lossless | contentPack | `buildCatalog.archetypes[].grantedVerbs[]` |  |
| `buildCatalog.archetypes[].id` | carried-lossless | contentPack | `buildCatalog.archetypes[].id` |  |
| `buildCatalog.archetypes[].name` | carried-lossless | contentPack | `buildCatalog.archetypes[].name` |  |
| `buildCatalog.archetypes[].progressionTreeId` | carried-lossless | contentPack | `buildCatalog.archetypes[].progressionTreeId` |  |
| `buildCatalog.archetypes[].resourceOverrides{}` | carried-lossless | contentPack | `buildCatalog.archetypes[].resourceOverrides{}` |  |
| `buildCatalog.archetypes[].startingInventory[]` | carried-lossless | contentPack | `buildCatalog.archetypes[].startingInventory[]` |  |
| `buildCatalog.archetypes[].startingTags[]` | carried-lossless | contentPack | `buildCatalog.archetypes[].startingTags[]` |  |
| `buildCatalog.archetypes[].statPriorities{}` | carried-lossless | contentPack | `buildCatalog.archetypes[].statPriorities{}` |  |
| `buildCatalog.backgrounds[].description` | carried-lossless | contentPack | `buildCatalog.backgrounds[].description` |  |
| `buildCatalog.backgrounds[].factionModifiers{}` | carried-lossless | contentPack | `buildCatalog.backgrounds[].factionModifiers{}` |  |
| `buildCatalog.backgrounds[].id` | carried-lossless | contentPack | `buildCatalog.backgrounds[].id` |  |
| `buildCatalog.backgrounds[].name` | carried-lossless | contentPack | `buildCatalog.backgrounds[].name` |  |
| `buildCatalog.backgrounds[].startingInventory[]` | carried-lossless | contentPack | `buildCatalog.backgrounds[].startingInventory[]` |  |
| `buildCatalog.backgrounds[].startingTags[]` | carried-lossless | contentPack | `buildCatalog.backgrounds[].startingTags[]` |  |
| `buildCatalog.backgrounds[].statModifiers{}` | carried-lossless | contentPack | `buildCatalog.backgrounds[].statModifiers{}` |  |
| `buildCatalog.crossTitles[].archetypeId` | carried-lossless | contentPack | `buildCatalog.crossTitles[].archetypeId` |  |
| `buildCatalog.crossTitles[].disciplineId` | carried-lossless | contentPack | `buildCatalog.crossTitles[].disciplineId` |  |
| `buildCatalog.crossTitles[].tags[]` | carried-lossless | contentPack | `buildCatalog.crossTitles[].tags[]` |  |
| `buildCatalog.crossTitles[].title` | carried-lossless | contentPack | `buildCatalog.crossTitles[].title` |  |
| `buildCatalog.disciplines[].description` | carried-lossless | contentPack | `buildCatalog.disciplines[].description` |  |
| `buildCatalog.disciplines[].drawback.amount` | carried-lossless | contentPack | `buildCatalog.disciplines[].drawback.amount` |  |
| `buildCatalog.disciplines[].drawback.stat` | carried-lossless | contentPack | `buildCatalog.disciplines[].drawback.stat` |  |
| `buildCatalog.disciplines[].drawback.type` | carried-lossless | contentPack | `buildCatalog.disciplines[].drawback.type` |  |
| `buildCatalog.disciplines[].grantedVerb` | carried-lossless | contentPack | `buildCatalog.disciplines[].grantedVerb` |  |
| `buildCatalog.disciplines[].id` | carried-lossless | contentPack | `buildCatalog.disciplines[].id` |  |
| `buildCatalog.disciplines[].name` | carried-lossless | contentPack | `buildCatalog.disciplines[].name` |  |
| `buildCatalog.disciplines[].passive.amount` | carried-lossless | contentPack | `buildCatalog.disciplines[].passive.amount` |  |
| `buildCatalog.disciplines[].passive.resource` | carried-lossless | contentPack | `buildCatalog.disciplines[].passive.resource` |  |
| `buildCatalog.disciplines[].passive.type` | carried-lossless | contentPack | `buildCatalog.disciplines[].passive.type` |  |
| `buildCatalog.disciplines[].requiredTags[]` | carried-lossless | contentPack | `buildCatalog.disciplines[].requiredTags[]` |  |
| `buildCatalog.entanglements[].archetypeId` | carried-lossless | contentPack | `buildCatalog.entanglements[].archetypeId` |  |
| `buildCatalog.entanglements[].description` | carried-lossless | contentPack | `buildCatalog.entanglements[].description` |  |
| `buildCatalog.entanglements[].disciplineId` | carried-lossless | contentPack | `buildCatalog.entanglements[].disciplineId` |  |
| `buildCatalog.entanglements[].effects[].tag` | carried-lossless | contentPack | `buildCatalog.entanglements[].effects[].tag` |  |
| `buildCatalog.entanglements[].effects[].type` | carried-lossless | contentPack | `buildCatalog.entanglements[].effects[].type` |  |
| `buildCatalog.entanglements[].id` | carried-lossless | contentPack | `buildCatalog.entanglements[].id` |  |
| `buildCatalog.maxTraits` | carried-lossless | contentPack | `buildCatalog.maxTraits` |  |
| `buildCatalog.requiredFlaws` | carried-lossless | contentPack | `buildCatalog.requiredFlaws` |  |
| `buildCatalog.statBudget` | carried-lossless | contentPack | `buildCatalog.statBudget` |  |
| `buildCatalog.traits[].category` | carried-lossless | contentPack | `buildCatalog.traits[].category` |  |
| `buildCatalog.traits[].description` | carried-lossless | contentPack | `buildCatalog.traits[].description` |  |
| `buildCatalog.traits[].effects[].amount` | carried-lossless | contentPack | `buildCatalog.traits[].effects[].amount` |  |
| `buildCatalog.traits[].effects[].stat` | carried-lossless | contentPack | `buildCatalog.traits[].effects[].stat` |  |
| `buildCatalog.traits[].effects[].type` | carried-lossless | contentPack | `buildCatalog.traits[].effects[].type` |  |
| `buildCatalog.traits[].id` | carried-lossless | contentPack | `buildCatalog.traits[].id` |  |
| `buildCatalog.traits[].incompatibleWith[]` | carried-lossless | contentPack | `buildCatalog.traits[].incompatibleWith[]` |  |
| `buildCatalog.traits[].name` | carried-lossless | contentPack | `buildCatalog.traits[].name` |  |
| `buildings[].buildingType` | no-channel | none | — | Placed enterable buildings have no pack field. |
| `buildings[].gridX` | no-channel | none | — | Placed enterable buildings have no pack field. |
| `buildings[].gridY` | no-channel | none | — | Placed enterable buildings have no pack field. |
| `buildings[].height` | no-channel | none | — | Placed enterable buildings have no pack field. |
| `buildings[].id` | no-channel | none | — | Placed enterable buildings have no pack field. |
| `buildings[].interiorZoneId` | no-channel | none | — | Placed enterable buildings have no pack field. |
| `buildings[].name` | no-channel | none | — | Placed enterable buildings have no pack field. |
| `buildings[].tags[]` | no-channel | none | — | Placed enterable buildings have no pack field. |
| `buildings[].width` | no-channel | none | — | Placed enterable buildings have no pack field. |
| `buildings[].zoneId` | no-channel | none | — | Placed enterable buildings have no pack field. |
| `category` | no-channel | none | — | Project category is not exported. A trap in both directions: the fixture value collides with `genre` (so a global value proof fails) and the key name collides with the buildCatalog trait `category` (so a key proof fails). Only the scoped proof states the actual claim. |
| `connections[].bidirectional` | no-channel | none | — | Typed inter-zone connections (kind / bidirectional / condition) have no pack field. Only the untyped `Zone.neighbors` id list crosses. |
| `connections[].condition` | no-channel | none | — | Typed inter-zone connections (kind / bidirectional / condition) have no pack field. Only the untyped `Zone.neighbors` id list crosses. |
| `connections[].fromZoneId` | no-channel | none | — | Typed inter-zone connections (kind / bidirectional / condition) have no pack field. Only the untyped `Zone.neighbors` id list crosses. |
| `connections[].kind` | no-channel | none | — | Typed inter-zone connections (kind / bidirectional / condition) have no pack field. Only the untyped `Zone.neighbors` id list crosses. |
| `connections[].label` | no-channel | none | — | Typed inter-zone connections (kind / bidirectional / condition) have no pack field. Only the untyped `Zone.neighbors` id list crosses. |
| `connections[].toZoneId` | no-channel | none | — | Typed inter-zone connections (kind / bidirectional / condition) have no pack field. Only the untyped `Zone.neighbors` id list crosses. |
| `craftingStations[].availableRecipes[]` | no-channel | none | — | World-placed crafting stations have no pack field, although the engine ships a `crafting-core` module. |
| `craftingStations[].id` | no-channel | none | — | World-placed crafting stations have no pack field, although the engine ships a `crafting-core` module. |
| `craftingStations[].stationType` | no-channel | none | — | World-placed crafting stations have no pack field, although the engine ships a `crafting-core` module. |
| `craftingStations[].zoneId` | no-channel | none | — | World-placed crafting stations have no pack field, although the engine ships a `crafting-core` module. |
| `description` | carried-lossless | packMeta | `description` | Also truncated to 100 chars into packMeta.tagline (convert-pack.ts:129). |
| `dialogues[].entryNodeId` | carried-lossless | contentPack | `dialogues[].entryNodeId` |  |
| `dialogues[].id` | carried-lossless | contentPack | `dialogues[].id` |  |
| `dialogues[].nodes{}.choices[].condition.params{}` | carried-lossless | contentPack | `dialogues[].nodes{}.choices[].condition.params{}` |  |
| `dialogues[].nodes{}.choices[].condition.type` | carried-lossless | contentPack | `dialogues[].nodes{}.choices[].condition.type` | A properly-shaped ConditionSpec — the shape zone exits fail to produce. |
| `dialogues[].nodes{}.choices[].effects[].params{}` | carried-lossless | contentPack | `dialogues[].nodes{}.choices[].effects[].params{}` |  |
| `dialogues[].nodes{}.choices[].effects[].target` | carried-lossless | contentPack | `dialogues[].nodes{}.choices[].effects[].target` |  |
| `dialogues[].nodes{}.choices[].effects[].type` | carried-lossless | contentPack | `dialogues[].nodes{}.choices[].effects[].type` |  |
| `dialogues[].nodes{}.choices[].id` | carried-lossless | contentPack | `dialogues[].nodes{}.choices[].id` |  |
| `dialogues[].nodes{}.choices[].nextNodeId` | carried-lossless | contentPack | `dialogues[].nodes{}.choices[].nextNodeId` |  |
| `dialogues[].nodes{}.choices[].text` | carried-lossless | contentPack | `dialogues[].nodes{}.choices[].text` |  |
| `dialogues[].nodes{}.effects[].params{}` | carried-lossless | contentPack | `dialogues[].nodes{}.effects[].params{}` |  |
| `dialogues[].nodes{}.effects[].target` | carried-lossless | contentPack | `dialogues[].nodes{}.effects[].target` |  |
| `dialogues[].nodes{}.effects[].type` | carried-lossless | contentPack | `dialogues[].nodes{}.effects[].type` |  |
| `dialogues[].nodes{}.id` | carried-lossless | contentPack | `dialogues[].nodes{}.id` |  |
| `dialogues[].nodes{}.nextNodeId` | carried-lossless | contentPack | `dialogues[].nodes{}.nextNodeId` |  |
| `dialogues[].nodes{}.speaker` | carried-lossless | contentPack | `dialogues[].nodes{}.speaker` |  |
| `dialogues[].nodes{}.text` | carried-lossless | contentPack | `dialogues[].nodes{}.text` | Node text stays a plain STRING here — unlike zone description, which is wrapped in a TextBlock array. The two halves of the pack disagree about how text is shaped. |
| `dialogues[].speakers[]` | carried-lossless | contentPack | `dialogues[].speakers[]` |  |
| `difficulty` | carried-approximated | packMeta | `difficulty` *(DIFFICULTY_MAP-with-silent-fallback)* | Six authored values collapse to three engine tiers; unmapped silently becomes `intermediate`. |
| `districts[].baseMetrics.commerce` | carried-lossless | contentPack | `districts[].baseMetrics.commerce` |  |
| `districts[].baseMetrics.morale` | carried-lossless | contentPack | `districts[].baseMetrics.morale` |  |
| `districts[].baseMetrics.safety` | carried-approximated | contentPack | `districts[].baseMetrics.surveillance` *(safety-renamed-to-surveillance)* | convert-districts.ts:26 assigns authored SAFETY to engine SURVEILLANCE. They are not synonyms — a heavily-surveilled district is not a safe one; in the engine's own doctrine high surveillance drives heat and pursuit. The value crosses; the meaning inverts. |
| `districts[].baseMetrics.stability` | carried-lossless | contentPack | `districts[].baseMetrics.stability` |  |
| `districts[].controllingFaction` | carried-lossless | contentPack | `districts[].controllingFaction` |  |
| `districts[].economyProfile.scarcityDefaults{}` | no-channel | none | — | See supplyCategories. |
| `districts[].economyProfile.supplyCategories[]` | no-channel | none | — | Dropped by convert-districts.ts:13-28 while the engine runs live per-district economies — the exact surface the 2.5D charter calls the moat. |
| `districts[].id` | carried-lossless | contentPack | `districts[].id` |  |
| `districts[].name` | carried-lossless | contentPack | `districts[].name` |  |
| `districts[].tags[]` | carried-lossless | contentPack | `districts[].tags[]` |  |
| `districts[].zoneIds[]` | carried-lossless | contentPack | `districts[].zoneIds[]` |  |
| `encounterAnchors[].cooldownTurns` | carried-lossless | contentPack | `encounterAnchors[].cooldownTurns` | Raw pass-through. |
| `encounterAnchors[].encounterType` | carried-lossless | contentPack | `encounterAnchors[].encounterType` | Raw pass-through. |
| `encounterAnchors[].enemyIds[]` | carried-lossless | contentPack | `encounterAnchors[].enemyIds[]` | Raw pass-through. |
| `encounterAnchors[].id` | carried-lossless | contentPack | `encounterAnchors[].id` | Raw pass-through. |
| `encounterAnchors[].probability` | carried-lossless | contentPack | `encounterAnchors[].probability` | Raw pass-through. |
| `encounterAnchors[].tags[]` | carried-lossless | contentPack | `encounterAnchors[].tags[]` | Raw pass-through. |
| `encounterAnchors[].zoneId` | carried-lossless | contentPack | `encounterAnchors[].zoneId` | Raw pass-through — and the ONLY entity-ish record whose zoneId survives export at all. |
| `entityPlacements[].ai.fears[]` | no-channel | none | — | Authored AI fears do not cross. Scoped: an authored fear ("flooding") collides with an unrelated `pressureHotspots[].pressureType` value, which an unscoped proof reads as carriage. |
| `entityPlacements[].ai.goals[]` | no-channel | none | — | Authored AI goals do not cross. |
| `entityPlacements[].ai.profileId` | carried-approximated | contentPack | `entities[].aiProfile` *(renamed-with-role-default-fallback)* | Carried when authored; otherwise SUBSTITUTED from ROLE_AI_PROFILE, so an unset field and an authored one are indistinguishable downstream. |
| `entityPlacements[].custom{}` | carried-lossless | contentPack | `entities[].custom{}` | Copied onto the blueprint under a key `EntityBlueprint` does not declare (cast through Record<string, unknown> at convert-entities.ts). |
| `entityPlacements[].dialogueId` | no-channel | none | — | The entity→dialogue binding is dropped even though BOTH sides cross: entities are exported and dialogues are exported, but nothing links them. |
| `entityPlacements[].entityId` | carried-lossless | contentPack | `entities[].id` *(renamed-key)* |  |
| `entityPlacements[].factionId` | carried-approximated | contentPack | `entities[].tags[]` *(encoded-as-faction-tag)* | A typed faction reference becomes the string `faction:<id>` inside a flat tag list. If the id is not declared in factionPresences it becomes the literal `faction:UNKNOWN` (with a warning) — the reference is destroyed, not just retyped. |
| `entityPlacements[].gridX` | no-channel | none | — | No coordinates cross. |
| `entityPlacements[].gridY` | no-channel | none | — | No coordinates cross. |
| `entityPlacements[].name` | carried-lossless | contentPack | `entities[].name` | Falls back to entityId when unset. |
| `entityPlacements[].portraitId` | carried-lossless | assetBindings | `entities{}.portraitId` | Binding-map channel only; not in the ContentPack. |
| `entityPlacements[].resources{}` | carried-lossless | contentPack | `entities[].baseResources{}` *(renamed-key)* | Omitted entirely when empty. |
| `entityPlacements[].role` | carried-approximated | contentPack | `entities[].type` *(ROLE_TO_TYPE-collapse)* | Six authored roles collapse to two engine types (convert-entities.ts:6): merchant / quest-giver / companion all become `npc`, boss becomes `enemy`. The distinction survives only indirectly, via the tags ROLE_TAGS adds. |
| `entityPlacements[].spawnCondition` | no-channel | none | — | The SpawnCondition grammar's ORIGINAL home field is dropped on export, while zone-exit conditions (a later borrower of the same grammar) are carried garbled. The grammar has no intact channel anywhere. |
| `entityPlacements[].spriteId` | carried-lossless | assetBindings | `entities{}.spriteId` | Binding-map channel only; not in the ContentPack. |
| `entityPlacements[].stats{}` | carried-lossless | contentPack | `entities[].baseStats{}` *(renamed-key)* | Omitted entirely when empty. |
| `entityPlacements[].tags[]` | carried-approximated | contentPack | `entities[].tags[]` *(merged-with-ROLE_TAGS-and-faction-tag)* | The exported tag list is authored tags UNION ROLE_TAGS[role] UNION `faction:<id>` — so it is a superset, and an importer cannot tell an authored `boss` tag from a role-derived one. |
| `entityPlacements[].zoneId` | no-channel | none | — | THE placement itself is dropped. `EntityBlueprint` has no location field, so an exported pack cannot say where any NPC stands — the single most consequential drop in the lane. Scoped to entities[] because `zoneId` DOES survive on the two raw pass-through domains. |
| `factionPresences[].alertLevel` | carried-lossless | contentPack | `factionPresences[].alertLevel` | Raw pass-through. |
| `factionPresences[].districtIds[]` | carried-lossless | contentPack | `factionPresences[].districtIds[]` | Raw pass-through. |
| `factionPresences[].factionId` | carried-lossless | contentPack | `factionPresences[].factionId` | Raw pass-through: export.ts:360 copies `project.factionPresences` unchanged into a pack key the ENGINE type does not declare. |
| `factionPresences[].influence` | carried-lossless | contentPack | `factionPresences[].influence` | Raw pass-through. |
| `factionPresences[].patrolRoutes[].zoneIds[]` | carried-lossless | contentPack | `factionPresences[].patrolRoutes[].zoneIds[]` | Raw pass-through. |
| `genre` | carried-approximated | packMeta | `genres[]` *(GENRE_MAP-with-silent-fallback)* | Mapped through GENRE_MAP (convert-pack.ts:8). `detective`→`mystery`, `zombie`→`post-apocalyptic`; an UNMAPPED genre silently becomes `fantasy` with no warning. |
| `hazardDefinitions[].blocksVision` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].effects[].amount` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].effects[].amountIsPercentMaxHp` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].effects[].chance` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].effects[].durationTicks` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].effects[].igniteChance` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].effects[].kind` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].effects[].stacking` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].effects[].statusId` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].effects[].tickOn` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].id` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].immuneTags[]` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].moveCostDelta` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].name` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].passable` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].tags[]` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].trigger` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hazardDefinitions[].weatherConditions[]` | no-channel | none | — | Typed hazard definitions have no pack field. Only the LEGACY free-text `Zone.hazards: string[]` crosses. |
| `hubs[].connectedZoneIds[]` | no-channel | none | — | Service/connectivity hubs have no pack field. |
| `hubs[].hubType` | no-channel | none | — | Service/connectivity hubs have no pack field. |
| `hubs[].id` | no-channel | none | — | Service/connectivity hubs have no pack field. |
| `hubs[].name` | no-channel | none | — | Service/connectivity hubs have no pack field. |
| `hubs[].serviceTypes[]` | no-channel | none | — | Service/connectivity hubs have no pack field. |
| `hubs[].tags[]` | no-channel | none | — | Service/connectivity hubs have no pack field. |
| `hubs[].zoneId` | no-channel | none | — | Service/connectivity hubs have no pack field. |
| `id` | carried-lossless | manifest | `id` | Also lands on packMeta.id, buildCatalog.packId, and manifest.contentPacks[]. |
| `itemPlacements[].container` | carried-approximated | contentPack | `items[].description` *(folded-into-synthesised-description)* | Container survives ONLY as prose inside a synthesised description, and ONLY when `description` is unset (convert-items.ts). Authoring both silently drops the container. The fixture exercises both branches. |
| `itemPlacements[].description` | carried-approximated | contentPack | `items[].description` *(synthesised-when-absent)* | When authored it crosses verbatim. When absent the exporter SYNTHESISES `Found in <container>` (or the literal `An item.`), so an unauthored description is indistinguishable from an authored one downstream. |
| `itemPlacements[].grantedTags[]` | carried-lossless | contentPack | `items[].grantedTags[]` | Omitted entirely when empty. |
| `itemPlacements[].grantedVerbs[]` | carried-lossless | contentPack | `items[].grantedVerbs[]` | Omitted entirely when empty. |
| `itemPlacements[].gridX` | no-channel | none | — | No coordinates cross. |
| `itemPlacements[].gridY` | no-channel | none | — | No coordinates cross. |
| `itemPlacements[].hidden` | carried-approximated | contentPack | `items[].provenance.flags[]` *(boolean-reencoded-as-contraband-flag)* | A boolean "hidden on the map" becomes the ECONOMIC flag `contraband` on item provenance. Only the true case is encoded; `hidden: false` writes nothing, so the field is not recoverable, and the meaning shifts from placement to legality. |
| `itemPlacements[].iconId` | carried-lossless | assetBindings | `items{}.iconId` | Binding-map channel only; not in the ContentPack. |
| `itemPlacements[].itemId` | carried-lossless | contentPack | `items[].id` *(renamed-key)* |  |
| `itemPlacements[].lootTableId` | no-channel | none | — | The loot-table reference is dropped along with the loot tables themselves. |
| `itemPlacements[].name` | carried-lossless | contentPack | `items[].name` | Falls back to itemId when unset. |
| `itemPlacements[].rarity` | carried-approximated | contentPack | `items[].rarity` *(narrowRarity-silent-fallback)* | Same silent-fallback shape as slot; the four schema rarities happen to match the engine set today, so nothing is lost YET. |
| `itemPlacements[].resourceModifiers{}` | carried-lossless | contentPack | `items[].resourceModifiers{}` | Omitted entirely when empty. |
| `itemPlacements[].slot` | carried-approximated | contentPack | `items[].slot` *(narrowSlot-silent-fallback)* | The schema declares SIX slots; convert-items accepts five. `consumable` — a legal authored slot — silently becomes `trinket`, with no warning and no fidelity entry. The fixture authors a consumable to prove it. |
| `itemPlacements[].statModifiers{}` | carried-lossless | contentPack | `items[].statModifiers{}` | Omitted entirely when empty. |
| `itemPlacements[].zoneId` | no-channel | none | — | Item PLACEMENT is dropped: `ItemDefinition` is a catalog record with no location. An exported pack knows every item and where none of them are. |
| `landmarks[].description` | no-channel | none | — | See name. |
| `landmarks[].gridX` | no-channel | none | — | No coordinates cross. |
| `landmarks[].gridY` | no-channel | none | — | No coordinates cross. |
| `landmarks[].iconId` | carried-lossless | assetBindings | `landmarks{}.iconId` | Same channel caveat: not part of the ContentPack. |
| `landmarks[].id` | carried-lossless | assetBindings | `landmarks{}` *(id-as-binding-map-key)* | A landmark id survives ONLY as a key in the asset binding map, and only when the landmark has an iconId. A landmark with no icon vanishes entirely. |
| `landmarks[].interactionType` | no-channel | none | — | Landmark interaction type does not cross. |
| `landmarks[].name` | no-channel | none | — | Points of interest do not reach the engine as content. |
| `landmarks[].tags[]` | no-channel | none | — | Landmark tags do not cross. |
| `landmarks[].zoneId` | no-channel | none | — | No landmark→zone link crosses. Scoped to the landmark's only surviving image (the binding map): `zoneId` DOES appear globally, on the raw-pass-through encounterAnchors and pressureHotspots, so a global proof would be false here while the claim is true. |
| `license` | no-channel | none | — | Project license is not exported. (Asset-pack `license` is a different field.) |
| `lootTables[].entries[].condition` | no-channel | none | — | Weighted loot tables have no pack field, although `itemPlacements[].lootTableId` can reference one. |
| `lootTables[].entries[].itemId` | no-channel | none | — | Weighted loot tables have no pack field, although `itemPlacements[].lootTableId` can reference one. |
| `lootTables[].entries[].quantity.max` | no-channel | none | — | Weighted loot tables have no pack field, although `itemPlacements[].lootTableId` can reference one. |
| `lootTables[].entries[].quantity.min` | no-channel | none | — | Weighted loot tables have no pack field, although `itemPlacements[].lootTableId` can reference one. |
| `lootTables[].entries[].rarity` | no-channel | none | — | Weighted loot tables have no pack field, although `itemPlacements[].lootTableId` can reference one. |
| `lootTables[].entries[].weight` | no-channel | none | — | Weighted loot tables have no pack field, although `itemPlacements[].lootTableId` can reference one. |
| `lootTables[].id` | no-channel | none | — | Weighted loot tables have no pack field, although `itemPlacements[].lootTableId` can reference one. |
| `lootTables[].rolls` | no-channel | none | — | Weighted loot tables have no pack field, although `itemPlacements[].lootTableId` can reference one. |
| `lootTables[].tags[]` | no-channel | none | — | Weighted loot tables have no pack field, although `itemPlacements[].lootTableId` can reference one. |
| `map.description` | no-channel | none | — | The authored grid (dimensions, tile size) has no pack field. The engine's space model is a graph; it has no coordinates to receive. |
| `map.gridHeight` | no-channel | none | — | The authored grid (dimensions, tile size) has no pack field. The engine's space model is a graph; it has no coordinates to receive. |
| `map.gridWidth` | no-channel | none | — | The authored grid (dimensions, tile size) has no pack field. The engine's space model is a graph; it has no coordinates to receive. |
| `map.id` | no-channel | none | — | The authored grid (dimensions, tile size) has no pack field. The engine's space model is a graph; it has no coordinates to receive. |
| `map.name` | no-channel | none | — | The authored grid (dimensions, tile size) has no pack field. The engine's space model is a graph; it has no coordinates to receive. |
| `map.tileSize` | no-channel | none | — | The authored grid (dimensions, tile size) has no pack field. The engine's space model is a graph; it has no coordinates to receive. |
| `marketNodes[].contrabandAvailable` | no-channel | none | — | Market nodes have no pack field, although the engine ships live per-district economies — the moat the 2.5D charter names. |
| `marketNodes[].id` | no-channel | none | — | Market nodes have no pack field, although the engine ships live per-district economies — the moat the 2.5D charter names. |
| `marketNodes[].merchantEntityId` | no-channel | none | — | Market nodes have no pack field, although the engine ships live per-district economies — the moat the 2.5D charter names. |
| `marketNodes[].priceModifier` | no-channel | none | — | Market nodes have no pack field, although the engine ships live per-district economies — the moat the 2.5D charter names. |
| `marketNodes[].supplyCategories[]` | no-channel | none | — | Market nodes have no pack field, although the engine ships live per-district economies — the moat the 2.5D charter names. |
| `marketNodes[].zoneId` | no-channel | none | — | Market nodes have no pack field, although the engine ships live per-district economies — the moat the 2.5D charter names. |
| `mode` | carried-approximated | packMeta | `tags[]` *(encoded-as-tag-prefix)* | The authoring MODE becomes the string `mode:<mode>` inside packMeta.tags (convert-pack.ts:124) — recoverable by convention, not by field. |
| `name` | carried-lossless | manifest | `title` *(renamed-key)* | manifest.title and packMeta.name both receive it verbatim. |
| `narratorTone` | carried-lossless | packMeta | `narratorTone` | Verbatim. |
| `playerTemplate.baseResources{}` | carried-lossless | contentPack | `playerTemplate.baseResources{}` |  |
| `playerTemplate.baseStats{}` | carried-lossless | contentPack | `playerTemplate.baseStats{}` |  |
| `playerTemplate.custom{}` | carried-lossless | contentPack | `playerTemplate.custom{}` |  |
| `playerTemplate.defaultArchetypeId` | carried-lossless | contentPack | `playerTemplate.defaultArchetypeId` |  |
| `playerTemplate.defaultBackgroundId` | carried-lossless | contentPack | `playerTemplate.defaultBackgroundId` |  |
| `playerTemplate.name` | carried-lossless | contentPack | `playerTemplate.name` |  |
| `playerTemplate.spawnPointId` | carried-lossless | contentPack | `playerTemplate.spawnPointId` | Crosses as a bare id whose referent (the SpawnPoint record) is dropped — a dangling reference by construction. |
| `playerTemplate.startingEquipment{}` | carried-lossless | contentPack | `playerTemplate.startingEquipment{}` |  |
| `playerTemplate.startingInventory[]` | carried-lossless | contentPack | `playerTemplate.startingInventory[]` |  |
| `playerTemplate.tags[]` | carried-lossless | contentPack | `playerTemplate.tags[]` |  |
| `pressureHotspots[].baseProbability` | carried-lossless | contentPack | `pressureHotspots[].baseProbability` | Raw pass-through. |
| `pressureHotspots[].id` | carried-lossless | contentPack | `pressureHotspots[].id` | Raw pass-through. |
| `pressureHotspots[].pressureType` | carried-lossless | contentPack | `pressureHotspots[].pressureType` | Raw pass-through. |
| `pressureHotspots[].tags[]` | carried-lossless | contentPack | `pressureHotspots[].tags[]` | Raw pass-through. |
| `pressureHotspots[].zoneId` | carried-lossless | contentPack | `pressureHotspots[].zoneId` | Raw pass-through. |
| `progressionTrees[].currency` | carried-lossless | contentPack | `progressionTrees[].currency` |  |
| `progressionTrees[].id` | carried-lossless | contentPack | `progressionTrees[].id` |  |
| `progressionTrees[].name` | carried-lossless | contentPack | `progressionTrees[].name` |  |
| `progressionTrees[].nodes[].cost` | carried-lossless | contentPack | `progressionTrees[].nodes[].cost` |  |
| `progressionTrees[].nodes[].description` | carried-lossless | contentPack | `progressionTrees[].nodes[].description` |  |
| `progressionTrees[].nodes[].effects[].params{}` | carried-lossless | contentPack | `progressionTrees[].nodes[].effects[].params{}` |  |
| `progressionTrees[].nodes[].effects[].target` | carried-lossless | contentPack | `progressionTrees[].nodes[].effects[].target` |  |
| `progressionTrees[].nodes[].effects[].type` | carried-lossless | contentPack | `progressionTrees[].nodes[].effects[].type` |  |
| `progressionTrees[].nodes[].id` | carried-lossless | contentPack | `progressionTrees[].nodes[].id` |  |
| `progressionTrees[].nodes[].name` | carried-lossless | contentPack | `progressionTrees[].nodes[].name` |  |
| `progressionTrees[].nodes[].requires[]` | carried-lossless | contentPack | `progressionTrees[].nodes[].requires[]` |  |
| `projectTags[]` | no-channel | none | — | Project discovery tags are not exported; packMeta.tags receives ONLY the `mode:` tag. Scoped because an authored tag also appears in an assetPack tag list. |
| `propPlacements[].gridX` | no-channel | none | — | Prop placements have no pack field. |
| `propPlacements[].gridY` | no-channel | none | — | Prop placements have no pack field. |
| `propPlacements[].id` | no-channel | none | — | Prop placements have no pack field. |
| `propPlacements[].propId` | no-channel | none | — | Prop placements have no pack field. |
| `propPlacements[].zoneId` | no-channel | none | — | Prop placements have no pack field. |
| `props[].height` | no-channel | none | — | Prop definitions have no pack field. |
| `props[].id` | no-channel | none | — | Prop definitions have no pack field. |
| `props[].imagePath` | no-channel | none | — | Prop definitions have no pack field. |
| `props[].interactable` | no-channel | none | — | Prop definitions have no pack field. |
| `props[].name` | no-channel | none | — | Prop definitions have no pack field. |
| `props[].tags[]` | no-channel | none | — | Prop definitions have no pack field. |
| `props[].walkable` | no-channel | none | — | Prop definitions have no pack field. |
| `props[].width` | no-channel | none | — | Prop definitions have no pack field. |
| `spawnPoints[].gridX` | no-channel | none | — | Spawn points are dropped as records; only `playerTemplate.spawnPointId` crosses, as a dangling id string. |
| `spawnPoints[].gridY` | no-channel | none | — | Spawn points are dropped as records; only `playerTemplate.spawnPointId` crosses, as a dangling id string. |
| `spawnPoints[].id` | no-channel | none | — | Spawn points are dropped as records; only `playerTemplate.spawnPointId` crosses, as a dangling id string. |
| `spawnPoints[].isDefault` | no-channel | none | — | Spawn points are dropped as records; only `playerTemplate.spawnPointId` crosses, as a dangling id string. |
| `spawnPoints[].zoneId` | no-channel | none | — | Spawn points are dropped as records; only `playerTemplate.spawnPointId` crosses, as a dangling id string. |
| `strata[].id` | no-channel | none | — | The v4.5 vertical-layer model has no pack field. Zero hits for `stratum`/`strata` anywhere in the engine repo. |
| `strata[].name` | no-channel | none | — | The v4.5 vertical-layer model has no pack field. Zero hits for `stratum`/`strata` anywhere in the engine repo. |
| `strata[].order` | no-channel | none | — | The v4.5 vertical-layer model has no pack field. Zero hits for `stratum`/`strata` anywhere in the engine repo. |
| `strata[].tags[]` | no-channel | none | — | The v4.5 vertical-layer model has no pack field. Zero hits for `stratum`/`strata` anywhere in the engine repo. |
| `strata[].visibleStrata[]` | no-channel | none | — | The v4.5 vertical-layer model has no pack field. Zero hits for `stratum`/`strata` anywhere in the engine repo. |
| `strata[].zRange.ceiling` | no-channel | none | — | The v4.5 vertical-layer model has no pack field. Zero hits for `stratum`/`strata` anywhere in the engine repo. |
| `strata[].zRange.floor` | no-channel | none | — | The v4.5 vertical-layer model has no pack field. Zero hits for `stratum`/`strata` anywhere in the engine repo. |
| `stratumLinks[].bidirectional` | no-channel | none | — | Cross-stratum connectors have no pack field. |
| `stratumLinks[].fromStratumId` | no-channel | none | — | Cross-stratum connectors have no pack field. |
| `stratumLinks[].fromZoneId` | no-channel | none | — | Cross-stratum connectors have no pack field. |
| `stratumLinks[].id` | no-channel | none | — | Cross-stratum connectors have no pack field. |
| `stratumLinks[].linkType` | no-channel | none | — | Cross-stratum connectors have no pack field. |
| `stratumLinks[].toStratumId` | no-channel | none | — | Cross-stratum connectors have no pack field. |
| `stratumLinks[].toZoneId` | no-channel | none | — | Cross-stratum connectors have no pack field. |
| `strongholds[].defenseLevel` | no-channel | none | — | Fortified faction seats have no pack field. |
| `strongholds[].factionId` | no-channel | none | — | Fortified faction seats have no pack field. |
| `strongholds[].garrisonEntityIds[]` | no-channel | none | — | Fortified faction seats have no pack field. |
| `strongholds[].id` | no-channel | none | — | Fortified faction seats have no pack field. |
| `strongholds[].name` | no-channel | none | — | Fortified faction seats have no pack field. |
| `strongholds[].tags[]` | no-channel | none | — | Fortified faction seats have no pack field. |
| `strongholds[].zoneId` | no-channel | none | — | Fortified faction seats have no pack field. |
| `tileLayers[].id` | no-channel | none | — | Tile layers have no pack field. |
| `tileLayers[].name` | no-channel | none | — | Tile layers have no pack field. |
| `tileLayers[].tiles[].gridX` | no-channel | none | — | Tile layers have no pack field. |
| `tileLayers[].tiles[].gridY` | no-channel | none | — | Tile layers have no pack field. |
| `tileLayers[].tiles[].tileId` | no-channel | none | — | Tile layers have no pack field. |
| `tileLayers[].zIndex` | no-channel | none | — | Tile layers have no pack field. |
| `tilesets[].id` | no-channel | none | — | Tilesets and their tiles have no pack field (visual layer; client-owned by the charter, but also unreachable by World Forge's own importer). |
| `tilesets[].imageHeight` | no-channel | none | — | Tilesets and their tiles have no pack field (visual layer; client-owned by the charter, but also unreachable by World Forge's own importer). |
| `tilesets[].imagePath` | no-channel | none | — | Tilesets and their tiles have no pack field (visual layer; client-owned by the charter, but also unreachable by World Forge's own importer). |
| `tilesets[].imageWidth` | no-channel | none | — | Tilesets and their tiles have no pack field (visual layer; client-owned by the charter, but also unreachable by World Forge's own importer). |
| `tilesets[].name` | no-channel | none | — | Tilesets and their tiles have no pack field (visual layer; client-owned by the charter, but also unreachable by World Forge's own importer). |
| `tilesets[].tileHeight` | no-channel | none | — | Tilesets and their tiles have no pack field (visual layer; client-owned by the charter, but also unreachable by World Forge's own importer). |
| `tilesets[].tiles[].col` | no-channel | none | — | Tilesets and their tiles have no pack field (visual layer; client-owned by the charter, but also unreachable by World Forge's own importer). |
| `tilesets[].tiles[].id` | no-channel | none | — | Tilesets and their tiles have no pack field (visual layer; client-owned by the charter, but also unreachable by World Forge's own importer). |
| `tilesets[].tiles[].opacity` | no-channel | none | — | Tilesets and their tiles have no pack field (visual layer; client-owned by the charter, but also unreachable by World Forge's own importer). |
| `tilesets[].tiles[].row` | no-channel | none | — | Tilesets and their tiles have no pack field (visual layer; client-owned by the charter, but also unreachable by World Forge's own importer). |
| `tilesets[].tiles[].tags[]` | no-channel | none | — | Tilesets and their tiles have no pack field (visual layer; client-owned by the charter, but also unreachable by World Forge's own importer). |
| `tilesets[].tiles[].tilesetId` | no-channel | none | — | Tilesets and their tiles have no pack field (visual layer; client-owned by the charter, but also unreachable by World Forge's own importer). |
| `tilesets[].tiles[].walkable` | no-channel | none | — | Tilesets and their tiles have no pack field (visual layer; client-owned by the charter, but also unreachable by World Forge's own importer). |
| `tilesets[].tileWidth` | no-channel | none | — | Tilesets and their tiles have no pack field (visual layer; client-owned by the charter, but also unreachable by World Forge's own importer). |
| `tones[]` | carried-approximated | packMeta | `tones[]` *(TONE_MAP-filtered)* | Mapped through TONE_MAP; unrecognised tones are dropped (warned), and an all-invalid list falls back to ['atmospheric']. |
| `transitions[].animation` | no-channel | none | — | Elevator / warp / lift transitions have no pack field. |
| `transitions[].durationSeconds` | no-channel | none | — | Elevator / warp / lift transitions have no pack field. |
| `transitions[].gridX` | no-channel | none | — | Elevator / warp / lift transitions have no pack field. |
| `transitions[].gridY` | no-channel | none | — | Elevator / warp / lift transitions have no pack field. |
| `transitions[].id` | no-channel | none | — | Elevator / warp / lift transitions have no pack field. |
| `transitions[].label` | no-channel | none | — | Elevator / warp / lift transitions have no pack field. |
| `transitions[].tags[]` | no-channel | none | — | Elevator / warp / lift transitions have no pack field. |
| `transitions[].targetZoneId` | no-channel | none | — | Elevator / warp / lift transitions have no pack field. |
| `transitions[].type` | no-channel | none | — | Elevator / warp / lift transitions have no pack field. |
| `transitions[].zoneId` | no-channel | none | — | Elevator / warp / lift transitions have no pack field. |
| `version` | carried-lossless | manifest | `version` | Also packMeta.version. Distinct from manifest.engineVersion, which is hard-coded. |
| `zones[].backgroundId` | carried-lossless | assetBindings | `zones{}.backgroundId` | Survives only in the World-Forge-side asset binding map, which is NOT part of the ContentPack the engine loads. |
| `zones[].collisionType` | no-channel | none | — | Collision channel hint: no channel. |
| `zones[].description` | carried-lossless | contentPack | `zones[].description[].text` *(wrapped-in-textblock)* | A plain string becomes `[{ text }]`. The conditional TextBlock form the engine schema allows is never produced. |
| `zones[].directionalLightPitch` | no-channel | none | — | Lighting hint: no channel. |
| `zones[].directionalLightYaw` | no-channel | none | — | Lighting hint: no channel. |
| `zones[].elevation` | no-channel | none | — | The 2.5D vertical field the charter's Pillar 2 names first. Zero hits for `elevation` in the engine repo. |
| `zones[].elevationRange.ceiling` | no-channel | none | — | Multi-level vertical span: no channel. |
| `zones[].elevationRange.floor` | no-channel | none | — | Multi-level vertical span: no channel. |
| `zones[].entryGate.conditions[]` | no-channel | none | — | The v4.5 party-state entry gate. The Godot lane consumes it; the engine lane has no field for it. |
| `zones[].entryGate.mode` | no-channel | none | — | hard-vs-soft gating: no channel. |
| `zones[].entryGate.reason` | no-channel | none | — | The authored "show the lock" message: no channel. |
| `zones[].exits[].condition` | carried-garbled | contentPack | `zones[].exits[].condition.type` *(whole-grammar-string-as-ConditionSpec.type)* | The SpawnCondition string (`item:rope`) is stuffed WHOLE into `ConditionSpec.type` with `params: {}` (convert-zones.ts:48). `parseSpawnCondition` is never called. `type` is meant to name a condition KIND, not carry its operands — so a valid ConditionSpec is produced that means nothing. |
| `zones[].exits[].label` | carried-lossless | contentPack | `zones[].exits[].label` |  |
| `zones[].exits[].targetZoneId` | carried-lossless | contentPack | `zones[].exits[].targetZoneId` |  |
| `zones[].gravityDirection` | no-channel | none | — | Physics override: no channel. |
| `zones[].gravityOverride` | no-channel | none | — | Physics override: no channel. |
| `zones[].gridHeight` | no-channel | none | — | See gridX. |
| `zones[].gridWidth` | no-channel | none | — | See gridX. |
| `zones[].gridX` | no-channel | none | — | No coordinates cross. Consistent with the charter's Pillar 2: fine space is client/authoring-owned. |
| `zones[].gridY` | no-channel | none | — | See gridX. |
| `zones[].hazardRefs[]` | no-channel | none | — | The TYPED hazard references. The legacy free-text `hazards` list crosses instead, so a zone that authored only typed hazards exports as hazard-free. |
| `zones[].hazards[]` | carried-lossless | contentPack | `zones[].hazards[]` | The LEGACY free-text hazard list. Carried verbatim; the typed `hazardDefinitions` + `hazardRefs` pair is not. |
| `zones[].id` | carried-lossless | contentPack | `zones[].id` |  |
| `zones[].interactables[].description` | no-channel | none | — | Interactable descriptions are lost in the name-only projection. |
| `zones[].interactables[].name` | carried-lossless | contentPack | `zones[].interactables[]` *(name-only-projection)* | The Interactable object collapses to its name string (convert-zones.ts:42). |
| `zones[].interactables[].type` | no-channel | none | — | Interaction type is lost in the name-only projection: the exported `interactables` is a bare string[], so no `type` key exists beneath it. Scoped because `type` is common elsewhere in the pack (EntityBlueprint.type, effect.type). |
| `zones[].light` | carried-lossless | contentPack | `zones[].light` |  |
| `zones[].name` | carried-lossless | contentPack | `zones[].name` |  |
| `zones[].neighbors[]` | carried-lossless | contentPack | `zones[].neighbors[]` | The one zone field that gates movement at runtime (traversal-core.ts:46). |
| `zones[].noise` | carried-lossless | contentPack | `zones[].noise` |  |
| `zones[].parallaxLayers[].assetRef` | no-channel | none | — | 2.5D parallax: no channel. (The referenced asset itself still appears in the `assets` manifest, which is why value-absence would not prove this.) |
| `zones[].parallaxLayers[].depth` | no-channel | none | — | 2.5D parallax: no channel. |
| `zones[].parallaxLayers[].id` | no-channel | none | — | 2.5D parallax: no channel. |
| `zones[].parallaxLayers[].scrollFactor` | no-channel | none | — | 2.5D parallax: no channel. |
| `zones[].parentDistrictId` | no-channel | none | — | The zone→district back-reference is dropped; only the district's own forward `zoneIds[]` list crosses, so the relation survives one-way by accident of the district converter. |
| `zones[].physicsMode` | no-channel | none | — | normal / platformer / zero-g / aquatic: no channel. |
| `zones[].skyAtmosphereRef` | no-channel | none | — | Sky preset: no channel. (Also has no referential validation on the authoring side — see the fixture note.) |
| `zones[].skyLightIntensity` | no-channel | none | — | Lighting hint: no channel. |
| `zones[].skylineRef` | no-channel | none | — | 2.5D vertical framing: no channel. |
| `zones[].stratumId` | no-channel | none | — | The zone→stratum membership link, dropped along with the strata themselves. |
| `zones[].tags[]` | carried-lossless | contentPack | `zones[].tags[]` | The single richest carried zone field — the engine reads tags as spawn, chokepoint, ambush and safe-zone gates. |
| `zones[].tilesetId` | carried-lossless | assetBindings | `zones{}.tilesetId` | Same channel caveat as backgroundId. |
| `zones[].timeOfDay` | no-channel | none | — | Time-of-day key: no channel — even though the SpawnCondition grammar has a `time:` operand the engine could gate on. |
